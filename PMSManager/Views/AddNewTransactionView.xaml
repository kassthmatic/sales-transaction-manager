<UserControl x:Class="PMSManager.Views.AddNewTransactionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Salesperson:" Margin="5"/>
        <ComboBox Grid.Column="1" Margin="5" ItemsSource="{Binding Employees}" 
                  SelectedItem="{Binding SelectedEmployee, Mode=TwoWay}"
                  DisplayMemberPath="Fname"/>

        <TextBlock Text="Product:" Grid.Row="1" Margin="5"/>
        <ComboBox Grid.Row="1" Grid.Column="1" Margin="5" ItemsSource="{Binding Products}" 
                  SelectedItem="{Binding SelectedProduct, Mode=TwoWay}"
                  DisplayMemberPath="Name"/>

        <TextBlock Text="Amount:" Grid.Row="2" Margin="5"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding Amount, Mode=TwoWay}"/>

        <TextBlock Text="Transaction Date:" Grid.Row="3" Margin="5"/>
        <DatePicker Grid.Row="3" Grid.Column="1" Margin="5" SelectedDate="{Binding SaleDate, Mode=TwoWay}"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button Content="Save" Width="100" Margin="10" Command="{Binding SaveCommand}"/>
            <Button Content="Cancel" Width="100" Margin="10" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>