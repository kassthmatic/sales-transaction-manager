<UserControl x:Class="PMSManager.Views.ListEditAllTransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800">
    <Grid Margin="10">
        <StackPanel>

            <TextBlock Text="SalesPerson:" FontWeight="Bold" Margin="0,0,0,5"/>
            <ComboBox ItemsSource="{Binding Employees}"
                      SelectedItem="{Binding SelectedEmployee, Mode=TwoWay}"
                      DisplayMemberPath="Fname"
                      Height="30"
                      Margin="0,0,0,10"/>

            <DataGrid ItemsSource="{Binding SalesTransactions}" AutoGenerateColumns="False" Height="300">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Product Id" Binding="{Binding ProductCode}" />
                    <DataGridTextColumn Header="Product Name" Binding="{Binding Product.Name}" />
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" />
                    <DataGridTextColumn Header="Sold time" Binding="{Binding SaleDate}" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>